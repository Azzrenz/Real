# 忽略规则
# 建立时间：2026-09-11 08:50（中国时区 UTC+8）· 仓库重建时新写
#
# 判断标准只有一条：**它是"人写的源码"还是"机器生成的产物/运行期私有数据"**。
# 只有前者进仓库。下面按类别列，每条都写清"为什么"——方便日后判断能不能删。

# ═══════════════════════════════════════════════════════════
# 一、编译产物（体积大、可重建、每次构建都变）
# ═══════════════════════════════════════════════════════════
# 用 target/ 而不是 /target：匹配**任意层级**的 target。
# （history：早期写 /target 只匹配根目录，server/target 漏网，13 GB 编译产物曾误入 git）
target/
**/*.rs.bk

node_modules/
dist/
dist-ssr/
client/src-tauri/target/

# ═══════════════════════════════════════════════════════════
# 二、密钥与本地环境（永不入库）
# ═══════════════════════════════════════════════════════════
.env
.env.local
.env.*.local
# 但示例文件必须留下（给协作者看需要填哪些变量）
!.env.example

# ═══════════════════════════════════════════════════════════
# 三、运行期数据（属于 Real 数据根 %APPDATA%\real-agent，不属于仓库）
# ═══════════════════════════════════════════════════════════
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.log
backend.log
hub.log
engine.log

# 运行态目录：.real 是 Real 自己的运行时沉淀；.agents 是自我复盘产物
.real/
.agents/

# 整个 server/data/ 目录忽略（不是只忽略 *.db）——
# 目录内还有脚本/文本等运行数据，只忽略文件类型会被 git add/clean 误伤
server/data/

# ═══════════════════════════════════════════════════════════
# 四、临时与误生成（历史上真出现过，逐条留痕防复发）
# ═══════════════════════════════════════════════════════════
# 构建警告捕获（build-run.bat 生成，非源码）
build_warnings.txt

# 前端临时产物
client/tmp/
client/build_out.txt
# UI 探针页：本地调样式用的一次性页面（内含演示文本，不入库）
client/scripts/probe/

# 根目录的 agent 运行态与临时产物
# （history：上面两条只写了 client/ 前缀，**根目录同名项漏网** ⇒
#  运行态存档与 tmp/ 下的探针脚本曾出现在 git status 里。
#  与文件开头记的「/target 只匹配根目录、server/target 漏网」是同一类错——
#  忽略规则必须**到处生效**，不能只覆盖某个子目录。）
/tmp/

# merge/rebase 冲突残留：*.orig 是合并前的原始版、*.mine 是本地版
# （history：server/.bak/ 下曾留 6 个 —— intask.rs / tests.rs / workflow.rs 各一对）
server/.bak/
*.orig
*.mine

# Windows 保留设备名（git 无法索引）
nul
dev/null

# cargo 全局缓存误入项目（CARGO_HOME 被误设时生成，曾提交 270 个 .crate）
toolscargo/

# 自改进脚本的事务隔离/临时编译目录
server/.self_fix_*/
server/.self_fix_*.json
**/*.tmpdir/

# Python 编译产物与隔离评分临时 workspace
__pycache__/
*.pyc
bench/tool_boundary_bench/complex_tasks/_iso/

# playwright 快照残留
server/.playwright-mcp/

# Tauri 构建自动生成的 schema（本地构建时重建）
client/src-tauri/gen/schemas/

# sidecar 产物：由 client/scripts/prepare-sidecar.mjs 从编译产物复制而来，
# 是「机器生成的产物」不是源码 —— 每次打包前重新生成即可，不进仓库。
client/src-tauri/binaries/

# 安装包产物
client/src-tauri/target/

# 捆绑运行时（体积大，由安装脚本 provision）
runtimes/

# ═══════════════════════════════════════════════════════════
# 五、事故留档（.git 损坏时的留档目录——是现场材料，不是源码）
# ═══════════════════════════════════════════════════════════
.git-broken-*/
.git-broken-*/*
ui/_theme-backup-*/

# ═══════════════════════════════════════════════════════════
# 六、IDE 与编辑器
# ═══════════════════════════════════════════════════════════
.idea/
.vscode/
*.swp
*.swo

# ═══════════════════════════════════════════════════════════
# 七、内部运营台账（不进公开仓库）
# ═══════════════════════════════════════════════════════════
# 判断标准同文件开头：这些是「内部运营内参」，不是「对外源码」。
# 内容形态：费用归因、成本调优数值、缓存策略实测、内部对照台账、
#   一次性诊断记录。对开源读者是噪音，对外则是成本结构与策略暴露。
# 处理方式：**本地物理保留**（写入工作流不变），仅从版本控制移除 ——
#   且**不放在仓库目录内**（开源前已整体移出）。
# ⚠️ 此处原先写作 `Docs/`，但 Windows 路径不区分大小写，会连带挡住公开文档用的
#   `docs/`，故移除该规则（2026-09-24）。内部台账请放在仓库目录之外。

# 一次性脚本与诊断产物（历史上散落在 Script/，2026-09-24 整体归档）
Script/
