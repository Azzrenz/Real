**结论要标清它是怎么来的**：同一句判断，来自"文件里写死的"和来自"跑起来看到的"，可信度不一样。

能把两边对上就对上（例：声明 `tb.h`=40 加工具栏 40 = 80，实际工具条 79px）；**对不上是发现，别抹平**。

**够不着的部分就划界**——一句话写清结论覆盖到哪、没覆盖什么，边界本身就是结论。

**有更硬的证据顺手就拿**：跑一次的代价通常低于猜一轮；用户已经给了的直接用。
