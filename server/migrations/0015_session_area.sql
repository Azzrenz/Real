-- 0015: 会话领域（任务列表的宏观标题）
--
-- 任务列表要的是「这件事属于哪一块」，聊天面板左上角要的是「当下在做什么」。
-- 两者共用一个 title 会让任务列表随每轮内容乱跳，所以拆成两列：
--   title = 具体任务名（左上角，由用户原话浓缩）
--   area  = 领域名（任务列表，由这一轮改动的文件路径推断）
-- 两列都只在用户没手工改过时才写。
ALTER TABLE sessions ADD COLUMN area TEXT;
