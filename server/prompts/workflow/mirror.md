<!-- #action -->
【行动事实 · 第 {rounds}/{max_rounds} 轮】
{facts}
- 已调用工具 {tool_calls} 次
- 本轮用户输入（原话）：{goal}

【方向对照】本轮用户输入**未必是任务指令**（可能是提问、纠正、或粘贴的引用）。若它与你在做的事
无关、或你指不出它说的是哪件事：先停手，一句话说你怎么理解它、依据是哪一句，让用户确认——不要猜着改。

【找不到就换路】一种找法试过两三次仍没结果，问题在路子不在次数。换找法（按内容搜而非按名字搜、
从调用方反查、从目录结构推）或换人问（把已确认的摆清楚，只问缺的那一个信息）；讲清哪条路试过、
为什么不通，比再说一遍「正在搜索」有用。

已知怎么修就直接落地最小修改、只改一次；改完跑一次既有测试验证，通过即收敛。

<!-- #progress -->
【进度摘要 · 第 {rounds}/{max_rounds} 轮（剩余 {left} 轮）】
- 已读文件：{reads}
- 已确认路径（照抄，禁止手动改写——下划线/连字符/大小写易错）：
{paths}
- 改动文件：{changed}
- 最近调用：{trace}
- 本轮用户输入（原话）：{goal}

对账后推进：已读的内容直接用，路径照上面清单抄；已改的地方不必重验；目标已达成即收敛。

<!-- #attribution -->
〔归因提示 · 第 {rounds} 轮〕

· 卡在哪：{tool}
· 发生了什么：{error}
· 原因分类：{category}
· 证据：{evidence}
· 对照：{contrast}
· 建议：{advice}

<!-- #terminate -->
⚠️ 任务由系统终止：{reason}

【事实记录】
- 已用轮次：{rounds}
- 工具调用：{tool_calls} 次
- 改动文件：{changed}
- 终态：未收敛（模型未给出完成信号）→ 按未完成归档

完整执行轨迹见会话的工具调用记录。
